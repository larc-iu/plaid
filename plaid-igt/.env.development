# Where plaid-ud runs in development. The jar serves it at /ud/, which is the
# built-in default; a dev session runs it on its own Vite port instead.
# The admin area's Projects tab links a UD project to the app that owns it.
VITE_UD_URL=http://localhost:5173
