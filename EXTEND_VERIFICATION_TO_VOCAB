TODO: extend the provenance / verification affordances to VOCABULARY (items).

Today provenance (machine vs human, confirm-on-edit, distinct rendering, bulk
"delete unconfirmed") is defined only for spans, relations, and vocab LINKS —
NOT vocab ITEMS. So a machine-created lexicon headword is indistinguishable
from a hand-curated one and can't be cleaned up as a batch.

Surfaced by the FST analyze service (services/igt_analyze_fst.py): it can
auto-create lexicon items for FST lemmas (gated by the `create_items` param,
default on). The items it makes are stamped prov:inferred, but nothing honors
that stamp for items.

- convention: provenance.py / provenance.js docstrings say "spans, relations,
  vocab links, optionally tokens" — add vocab items.
- ui (plaid-igt): VocabularyItems.jsx / VocabularyDetail.jsx — render machine
  items distinctly (violet/dashed like links), verify-on-edit, and a
  "delete unconfirmed" bulk action.
- query: be able to filter vocab items by prov state.
- relates to MAKE_BULK_VOCAB_ITEM (bulk cleanup needs the bulk endpoint).

Surfaced 2026-06-16 building the FST demo service.
