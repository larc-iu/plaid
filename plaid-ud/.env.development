# Where plaid-igt runs in development. The jar serves it at /igt/, which is the
# built-in default; a dev session runs it on its own Vite port instead.
# UD links to plaid-igt's admin area rather than growing a second one, since the
# release jar always ships both apps on one server.
VITE_IGT_URL=http://localhost:5174
